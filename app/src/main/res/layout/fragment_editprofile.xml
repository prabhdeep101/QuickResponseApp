<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/backgroundphoto">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Profile Image -->
        <ImageView
            android:id="@+id/editProfileImage"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginTop="16dp"
            android:src="@drawable/kaurihead"
            android:contentDescription="Profile Image"
            android:background="@drawable/bg_profile_border"
            android:padding="4dp"
            android:layout_gravity="center" />

        <!-- Section Title -->
        <TextView
            android:text="EDIT PROFILE"
            android:textStyle="bold"
            android:layout_marginTop="16dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/black" />

        <!-- Name -->
        <EditText
            android:id="@+id/editName"
            android:hint="NAME / INGOA"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="12dp"
            android:background="@android:drawable/edit_text" />

        <!-- Address -->
        <EditText
            android:id="@+id/editAddress"
            android:hint="CURRENT ADDRESS / WĀHI NOHO O NĀIANEI"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="12dp"
            android:background="@android:drawable/edit_text" />

        <!-- Guardian Section -->
        <TextView
            android:text="GUARDIAN / KAITIAKI"
            android:textStyle="bold"
            android:layout_marginTop="16dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/black" />

        <EditText
            android:id="@+id/editGuardianName"
            android:hint="NAME / INGOA"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="12dp"
            android:background="@android:drawable/edit_text" />

        <EditText
            android:id="@+id/editGuardianPhone"
            android:hint="PHONE NUMBER / NAMA WAEA"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="12dp"
            android:inputType="phone"
            android:background="@android:drawable/edit_text" />

        <!-- Checkbox -->
        <CheckBox
            android:id="@+id/editOrangaCheck"
            android:text="IS THIS PERSON APPOINTED BY ORANGA TAMARIKI?"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="12dp"
            android:textColor="@android:color/black" />

        <!-- Save Button -->
        <Button
            android:id="@+id/updateProfileButton"
            android:text="SAVE"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:backgroundTint="#FBB03B"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:layout_gravity="center_horizontal" />
    </LinearLayout>
</ScrollView>